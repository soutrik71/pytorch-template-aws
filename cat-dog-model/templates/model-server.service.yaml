apiVersion: v1
kind: Service
metadata:
  name: model-server-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: model-server
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP  # Internal cluster access
